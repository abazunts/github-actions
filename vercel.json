{
  "version": 2,
  "name": "github-actions",
  "builds": [
    { "src": "src/(.*)", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "(.*)", "dest": "src/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
